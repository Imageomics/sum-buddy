repos:
  - repo: local
    hooks:
      - id: ruff
        name: ruff
        entry: uv run --extra dev ruff check --force-exclude
        language: system
        types_or: [python, pyi]
        require_serial: true
